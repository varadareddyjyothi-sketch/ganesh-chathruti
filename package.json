{
  "name": "ganesh-blessings-root",
  "version": "1.0.0",
  "private": true,
  "description": "Ganesh Blessings - Ask a Wish Fullstack Application",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "build": "npm run build --prefix frontend",
    "start:backend": "npm start --prefix backend"
  },
  "dependencies": {
    "firebase": "^12.19.0"
  }
}
